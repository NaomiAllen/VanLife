<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1><%=blog.title %> </h1>
        <br>
        located in <%=blog.location %>.
        <% if(blog.blogComplete === true){ %>
            This masterpiece is complete!
        <% } else { %>
            This Masterpiece is still needs some work
        <% } %>
        <nav>
            <a href="/blogs">Back to Blogs Index</a>
        </nav>
        <main>
            <section>
                <%=blogs.body%>
                <a href="/blogs/<%=blogs._id%>/edit">Edit</a>
                <form action="/blogs/<%=blogs._id%>?_method=DELETE" method="post">
                    <input type="submit" value="Delete Blog"/>
                </form>
            </section>
        </main>
    </body>
</html>